# Flutter's generated Android configuration supplies the framework keep rules.
# Keep TiCash's application entry point and model metadata used by plugins.
-keep class com.ticash.app.** { *; }
-keepattributes *Annotation*
